import { afterEach, describe, expect, it, vi } from 'vitest'; import { fetchUnityAssetStoreListing } from './unityAssetStore.provider.js';
afterEach(() => vi.unstubAllGlobals()); describe('Unity provider', () => { it('converts a network timeout into a safe error', async () => { vi.stubGlobal('fetch', (_url: string, init: RequestInit) => new Promise((_resolve, reject) => init.signal?.addEventListener('abort', () => reject(new DOMException('Aborted', 'AbortError'))))); await expect(fetchUnityAssetStoreListing('https://assetstore.unity.com/packages/test-1', 5)).rejects.toThrow('timed out'); }); });
