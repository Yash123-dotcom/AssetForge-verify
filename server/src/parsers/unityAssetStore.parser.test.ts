import { readFileSync } from 'node:fs'; import { fileURLToPath } from 'node:url'; import { describe, expect, it } from 'vitest'; import { parseUnityAssetStore } from './unityAssetStore.parser.js';
const fixture = (name: string) => readFileSync(fileURLToPath(new URL(`./__fixtures__/${name}`, import.meta.url)), 'utf8'); const url = 'https://assetstore.unity.com/packages/vfx/example-123';
describe('Unity Asset Store parser', () => { it('extracts explicit URP metadata and shader hints', () => { const result = parseUnityAssetStore(fixture('urp-listing.html'), url); expect(result).toMatchObject({ assetName: 'Stylized Fire', publisherName: 'Example Studio', category: 'VFX', unityVersion: '2022', pipelineSupport: ['URP'], dependencies: ['Cinemachine'], customShaders: true, packageVersion: '1.2.0' }); }); it('extracts multiple explicitly compatible pipelines', () => { expect(parseUnityAssetStore(fixture('multi-listing.html'), url).pipelineSupport).toEqual(['BUILT_IN','URP','HDRP']); }); it('returns unknown rather than inventing missing data', () => { const result = parseUnityAssetStore(fixture('partial-listing.html'), url); expect(result.pipelineSupport).toEqual([]); expect(result.unityVersion).toBeNull(); expect(result.customShaders).toBe('UNKNOWN'); }); });
