{
  "name": "assetforge-verify",
  "private": true,
  "scripts": {
    "dev": "concurrently -n client,server -c green,cyan \"npm run dev --prefix client\" \"npm run dev --prefix server\"",
    "build": "npm run build --prefix server && npm run build --prefix client",
    "typecheck": "npm run typecheck --prefix server && npm run typecheck --prefix client",
    "lint": "npm run lint --prefix server && npm run lint --prefix client",
    "test": "npm test --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
